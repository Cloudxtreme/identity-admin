include file("/etc/gu/identity-admin.conf")

play.http.session.secure=true
play.http.filters = "filters.Filters"

play.i18n.langs = ["en"]

identity-admin {
  groups {
    useradmin = "identity.userhelp@guardian.co.uk"
  }
}

akka {
  loggers = ["akka.event.slf4j.Slf4jLogger"]
  loglevel = "DEBUG"
}
